# torch>=2.1
# torchvision>=0.16
timm>=1.0.0
opencv-python>=4.8
Pillow>=10.0
PyYAML>=6.0
scikit-learn>=1.3
tqdm>=4.66
requests>=2.31
# MTCNN compatibility install (run only after the requirements above):
# python -m pip install --no-deps facenet-pytorch==2.6.0
